{
  "name": "kanban-oueb",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest --reporter=json --outputFile=report.json"
  },
  "devDependencies": {
    "vite": "^5.0.8",
    "vitest": "^1.0.4"
  }
}
